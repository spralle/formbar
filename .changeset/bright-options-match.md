---
"@formbar/from-schema": minor
---

Add typed `x-formbar.options` normalization, per-field normalized choices, title resolution, and observable warnings while preserving standard schema validation semantics.
